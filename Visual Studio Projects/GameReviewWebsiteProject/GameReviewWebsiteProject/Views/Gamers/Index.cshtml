@model IEnumerable<GameReviewWebsiteProject.Models.Gamer>

@{
    ViewBag.Title = "Gamers";
}

<h2>Gamers</h2>
@using (Html.BeginForm())
{
    @Html.TextBox("search")
    <input type="submit" />
}
@if (!String.IsNullOrEmpty(ViewBag.PreviewSearch))
{
    <h2>You are searching for @ViewBag.PreviewSearch</h2>
    <strong style="color: red;">@ViewBag.SearchError</strong>
}
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AvatarUrl)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                <h2>@Html.ActionLink(item.Name, "Details", new { id = item.GamerId })</h2>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AvatarUrl)
            </td>
        </tr>
    }

</table>
